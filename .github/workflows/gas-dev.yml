name: GAS Dev Push

on:
  push:
    branches: [ "main" ]
    # 変更ファイルを限定したければ paths を有効化:
    # paths:
    #   - "src/**"
    #   - ".clasp.json"
    #   - "package.json"
    #   - "yarn.lock"

jobs:
  push-dev:
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install deps
        run: |
          yarn --version
          yarn install --frozen-lockfile || yarn install --immutable

      - name: Push to Apps Script
        run: yarn push